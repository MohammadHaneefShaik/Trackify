<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= title %>
  </title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      background: #f4f6f9;
      font-family: "Poppins", sans-serif;
      color: #333;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Navbar */
    .navbar {
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
    }

    .navbar-brand {
      font-weight: 600;
      font-size: 1.4rem;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .navbar-brand i {
      color: #00b4d8;
      font-size: 1.5rem;
    }

    /* Navbar toggle icon */
    .navbar-toggler {
      border: none;
      outline: none;
    }

    .navbar-toggler:focus {
      box-shadow: none;
    }

    .navbar-toggler-icon {
      background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='%23fff' viewBox='0 0 30 30'%3e%3cpath stroke='rgba(255,255,255, 0.7)' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
    }

    /* Container */
    .container {
      max-width: 950px;
      flex: 1;
    }

    /* Card styling */
    .card {
      border: none;
      border-radius: 12px;
      transition: all 0.3s ease;
    }

    .card:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
    }

    /* Buttons */
    .btn {
      border-radius: 8px;
      font-weight: 500;
      letter-spacing: 0.3px;
    }

    .btn-brand {
      background: linear-gradient(90deg, #00b4d8, #00a5c6);
      color: white !important;
      border: none;
      font-weight: 600;
      border-radius: 10px;
      transition: 0.2s ease;
      display: inline-block;
      text-align: center;
    }

    .btn-brand:hover {
      box-shadow: 0 6px 16px rgba(0, 180, 216, 0.3);
      transform: translateY(-1px);
    }

    /* Form styling */
    input,
    select {
      border-radius: 8px !important;
    }

    .form-control:focus,
    .form-select:focus {
      border-color: #007bff;
      box-shadow: 0 0 0 0.15rem rgba(0, 123, 255, 0.25);
    }

    /* Map styling */
    #map {
      border-radius: 12px;
      border: 1px solid #ddd;
    }

    /* Alerts */
    .alert {
      border-radius: 10px;
    }


    /* Footer */
    footer {
      background: #222;
      color: #ccc;
      text-align: center;
      padding: 12px 0;
      font-size: 0.9rem;
      letter-spacing: 0.4px;
      margin-top: auto;
    }

    footer span {
      color: #00a5c6;
      font-weight: 600;
    }

    /* Mobile adjustments */
    @media (max-width: 768px) {
      navbar-collapse {
        text-align: center;
      }

      .navbar-nav {
        width: 100%;
        justify-content: center;
        align-items: center;
      }

      .navbar-nav .nav-item {
        margin-bottom: 8px;
      }

      .navbar-nav .btn {
        width: 120px;
      }

      .navbar-brand i {
        font-size: 1.3rem;
      }

      footer {
        font-size: 0.9rem;
        padding: 12px 0;
      }

      h3,
      h4 {
        font-size: 1.1rem;
      }

      .container {
        padding: 10px;
      }
    }

    @media (max-width: 480px) {
      .navbar-brand {
        margin-left: 32%;
      }

      navbar-collapse {
        text-align: center;
      }

      .navbar-nav {
        width: 100%;
        justify-content: center;
        align-items: center;
      }

      .navbar-nav .nav-item {
        margin-bottom: 8px;
      }

      .navbar-nav .btn {
        width: 120px;
      }

      .navbar-brand i {
        font-size: 1.3rem;
      }

      footer {
        font-size: 0.8rem;
        padding: 10px 0;
        line-height: 1.4;
      }

      footer span {
        display: block;
        margin-top: 4px;
      }
    }
  </style>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <i class="fa-solid fa-bus"></i> Trackify
      </a>

      <!-- Hamburger button -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav align-items-lg-center">
          <% if (user) { %>
            <li class="nav-item">
              <span class="nav-link text-white fw-semibold">
                <%= user.name %> (<%= user.role %>)
              </span>
            </li>
            <li class="nav-item">
              <a href="/logout" class="btn btn-danger btn-sm px-3 ms-lg-2 mb-2 mb-lg-0">Logout</a>
            </li>
            <% } else { %>
              <li class="nav-item">
                <a href="/auth/login" class="btn btn-light btn-sm px-3 ms-lg-2 mb-2 mb-lg-0">Login</a>
              </li>
              <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container py-4">
    <% if (page) { %>
      <%- include(page) %>
        <% } %>
  </div>

  <footer>
    © <%= new Date().getFullYear() %> — Designed & Developed by
      <span><a href="https://www.linkedin.com/in/shaik-mohammad-haneef-6b3686309/">Shaik Mohammad Haneef</a></span>
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>